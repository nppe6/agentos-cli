{
  "name": "agentos-cli",
  "version": "1.0.0",
  "description": "CLI for injecting Agent OS workflow files into existing projects.",
  "main": "bin/cli.js",
  "bin": {
    "agentos-cli": "bin/cli.js"
  },
  "scripts": {
    "test": "node --test"
  },
  "keywords": [
    "agent-os",
    "cli"
  ],
  "license": "ISC",
  "dependencies": {
    "commander": "^13.1.0",
    "inquirer": "^12.3.2"
  }
}
