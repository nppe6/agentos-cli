# Shelf Context

Load the current AgentOS Shelf workspace context before continuing.

Run:

```bash
agentos-cli shelf workspace context
```

Then summarize:

- current developer
- git branch and working tree state
- current task
- active tasks
- active journal file
- available spec layers

If Shelf is not initialized or Python is missing, explain the exact command the user should run next.
