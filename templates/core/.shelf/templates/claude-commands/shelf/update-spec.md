# Shelf Update Spec

Update durable AgentOS Shelf project knowledge after a completed change.

Review:

- the completed task under `.shelf/tasks/`
- changed files
- relevant existing `.shelf/spec/` documents
- session notes under `.shelf/workspace/`

Only move durable, reusable project knowledge into `.shelf/spec/`. Keep one-off task notes in `.shelf/tasks/` and session narrative in `.shelf/workspace/`.

After editing spec files, summarize exactly which project rules changed and why.
