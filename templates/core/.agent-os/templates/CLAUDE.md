# Project Agent OS Rules

This project uses `.agent-os/` as the shared source for agent workflow rules and skills.

Load the relevant project skills for the current task, protect existing user changes, and keep platform-specific behavior in generated Claude Code projections rather than in the shared source.
