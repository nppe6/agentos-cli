# Agent OS

`.agent-os/` is the shared source of truth for agent rules, skills, specs, and lightweight project memory.

Platform-specific files such as `AGENTS.md`, `CLAUDE.md`, `.codex/`, and `.claude/` are generated projections.
